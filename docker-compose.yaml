services:
  net-finder:
    image: jollaman999/net-finder:3.0.3
    container_name: net-finder
    network_mode: host
    cap_add:
      - NET_RAW
      - NET_ADMIN
    volumes:
      - ./config:/root/.config/net-finder
      - /etc/machine-id:/etc/machine-id:ro
    restart: always
